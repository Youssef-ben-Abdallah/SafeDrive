name: safedrive

description: >-
  SafeDrive – early Flutter application scaffold for detecting driver drowsiness and distraction.

publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  camera: ^0.10.5+9
  permission_handler: ^11.3.1
  firebase_core: ^2.24.2
  google_mlkit_commons: ^0.9.0
  google_mlkit_face_detection: ^0.11.0
  google_mlkit_object_detection: ^0.12.0
  flutter_ringtone_player: ^3.2.0
  vibration: ^1.8.4
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
    - assets/lang/
